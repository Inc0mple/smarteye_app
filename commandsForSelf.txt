python -m venv virtenv
python -m venv newenv
pip install -r requirements.txt

python fall_detector.py --resolution 0.35
python fall_detector.py --resolution 0.4
python fall_detector_backup.py --resolution 0.3
deactivate
virtenv\Scripts\activate
newenv\Scripts\activate

names = ['John Smith','Rebecca Briggs']
usernames = ['jsmith','rbriggs']
passwords = ['123','456']

try incorporating https://github.com/hassancpu/Fall-Detection/blob/master/main.py 

streamlit run app.py

changes done:

virtenv>Lib>site-packages>openpifpaf>decoder>cifcaf.py>comment out line 119 (assert not cls.force_complete)


"""
https://www.db4free.net/ (Free MySQL host for deployment tests?)
TODO:

11/1/2022:

- DONE properly store hashed passwords 
- DONE implement and test signups
- DONE implement functions to retrieve tables
- DONE Design config and eventLog tables
- DONE Ability to retrieve and display selected tables in app (config, eventLog)
- DONE implemement logging of falls using EventLog and Config information
- DONE implement alerting of users via email + email verification
- DONE implement in-app config changes/updates
- DONE implement routine event logging/emailing
- DONE implement ability to change email/phone 
- DONE convert to MySQL syntax instead of sqllite
- UI cleanup + any necessary refactors
    Refactors: 
    DONE seperate admin page from normal user page
    DONE proper personalised page to check event logs
    DONE Universal logout button
    Rename implementation
- Deployment 1
- Deployment 2
- Deployment 3
- Include in portfolio

After-deployment features:
- Implemement Guest function
- * Improve fall detection abilties
- Make webcam work on mobile
"""

"""
    SELECT user.username as u, user.userId AS id
    FROM user
    INNER JOIN config ON id = config.userId
    WHERE u=?;
"""